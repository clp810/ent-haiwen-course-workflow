# Contributing

本仓库优先接受小而可验证的改动。每个改动应说明影响的是分析 Skill、交付 Skill、确定性脚本还是隐私规则，并提供不含真实课程或个人数据的复现方式。

## 提交流程

1. 从新分支开始，只修改与问题有关的文件。
2. 使用完全虚构的测试数据。不要提交真实课件、真实 `WEEK_HANDOFF.md`、提交物、共享链接、成员或导师姓名、访谈原文、录音、凭据或本机绝对路径。
3. 如果修改 Skill 行为，同时更新相应 reference 和测试；不要把所有细节堆回 `SKILL.md`。
4. 运行 `./scripts/test.sh`、`./scripts/validate_skills.sh` 和 `python3 scripts/audit_repository.py`。
5. 通过 “Skill feedback” Issue 描述问题、预期行为、实际行为和经过净化的复现步骤。

## 设计约束

- 两个 Skill 必须从 repo root 发现文件，不依赖成员用户目录。
- 分析只读当前周材料和必要的共享说明；不要无故扫描其他周。
- 最多提出 6 个分组问题，每个问题应关闭一个重要信息缺口。
- `WEEK_HANDOFF.md` 是唯一每周协调记录，不复制整份课件。
- DRAFT 文件名稳定且唯一；FINAL 需要明确确认并使用 `_vN_FINAL`。
- 课程事实、团队解释、建议、未知项和待确认项必须区分。
- 外部上传、LMO 提交、云文档修改和消息发送不属于本仓库默认权限。

## 修改模板

课程模板只有在课程或版权所有方允许时才能提交。替换模板前先清除 Office 个人元数据，再运行全套测试和审计。不要把已填写的工作簿当作模板提交。
